import React from "react";
import { View, Text, StyleSheet, Image } from "react-native";

const SortingForm = () => {
    return (
        <View>
            <Text>SortingForm</Text>
        </View>
    );
};

const styles = StyleSheet.create({

});

export default SortingForm;