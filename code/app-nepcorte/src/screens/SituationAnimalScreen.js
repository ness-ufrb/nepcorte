import React from 'react';
import { View, Text, StyleSheet, Button, TouchableOpacity } from 'react-native';

const SituationAnimalScreen = () => {
    return (
        <View>           
            <Text style={{fontSize: 30}}>SituationAnimalScreen</Text>
            {/* <TouchableOpacity onPress={console.log("algo")}>
                <Text>Algo</Text>
            </TouchableOpacity> */}
            {/* <Button title="Go to next screen" onPress={console.log("algo")}/> */}
        </View>
    )
};

const styles = StyleSheet.create({
});

export default SituationAnimalScreen;
